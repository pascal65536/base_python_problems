def sum_of_numbers(numbers):
    total = 0
    for num in numbers:
        total += num
    return total

# Пример использования
numbers = [1, 2, 3, 4, 5]
print("Сумма чисел:", sum_of_numbers(numbers))  # Вывод: 15
